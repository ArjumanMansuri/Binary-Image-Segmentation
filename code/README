
Prerequisites:

cmake, opencv


To compile in the lab
——————————

bash ./lab_config.sh
cd build
make

To compile on your own machine
———

cd build/
cmake ../
make


To run
——

In the build folder:
./seg simple.png config.txt output.png

 